name: Pull Request Checker

on:
  pull_request:
    branches: [master]
  workflow_dispatch:

jobs:
  pipeline-checker:
    name: Pull Request Checker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 2.7.8

      - name: Config Gemfile for checker
        run: |
          mv Gemfile Gemfilee
          mv GemfileForChecker Gemfile

      - name: Install Bundler
        run: |
          gem install bundler -v '2.4.22'

      - name: Bundle Install
        run: bundle install

      - name: Execute code detector
        run: |
          chmod +x $(which reek)
          reek || (echo "Reek failed with warnings/errors" && exit 1)

      - name: Execute code static analyzer
        run: |
          rubocop --fail-level warning || (echo "Rubocop failed with warnings/errors" && exit 1)